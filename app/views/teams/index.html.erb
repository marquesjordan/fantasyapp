<h1>Listing teams</h1>

<table>
  <thead>
    <tr>
      <th>Nba team</th>
      <th>Team name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%# @teams.each do |team| %>
      <tr>
        <td><%#= team.nba_team_id %></td>
        <td><%#= team.team_name %></td>
        <td><%#= link_to 'Show', team %></td>
        <td><%#= link_to 'Edit', edit_team_path(team) %></td>
        <td><%#= link_to 'Destroy', team, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <%# end %>
  </tbody>
</table>
<%#= @teamuri['league']['season']['conference'][0]['division'][0]['team'][0]['name'] %>
<br>

<%= link_to 'New Team', new_team_path %>
